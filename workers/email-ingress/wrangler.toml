name = "blip-email-ingress"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[vars]
API_BASE_URL = "https://api.useblip.email"

# WORKER_SECRET should be set via `wrangler secret put WORKER_SECRET`
